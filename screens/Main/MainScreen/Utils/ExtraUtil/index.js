import React, { useState } from 'react';
import { View, Text } from 'react-native';

const IRPF = () => {
    return (
        <View style={{ backgroundColor: '#514242', alignItems: 'center', justifyContent: 'center', flex: 1 }}>
            <Text style={{ fontSize: 18, color: '#F1F1F1' }}> Ajuste futuro </Text>
        </View>
    );
};

export default IRPF;